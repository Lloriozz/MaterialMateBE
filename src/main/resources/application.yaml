server:
  port: 8080
  servlet:
    context-path: /mm

spring:
  datasource:
    url: "jdbc:mysql://localhost:3307/MaterialMate"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080
  servlet:
    context-path: /mm

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/materialmatetest"
    username: root
    password: Khanh07062005.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true